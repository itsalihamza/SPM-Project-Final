# Minimal dependencies for Railway deployment
# Compatible with Python 3.13

# Core API
fastapi==0.121.3
uvicorn==0.38.0
pydantic==2.5.0
requests==2.31.0
structlog==23.1.0

# Web scraping
selenium==4.38.0
webdriver-manager==4.0.2
beautifulsoup4==4.12.0

# Analytics (minimal)
numpy>=1.26.0,<2.0.0
scikit-learn==1.3.2

# Note: Removed matplotlib and pandas due to Python 3.13 compatibility issues
# Reports will be JSON only (no CSV or visual charts on Railway)
